#pragma once

/**
 * Client side elements refers to all the elements that are accessible from a client app
 * So, the cache itself and the put/get commands
 *
 * Client side entities are managed from the supervisor
 */

#include <client/entity.hh>
